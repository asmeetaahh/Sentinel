# On macOS, xgboost also requires the OpenMP runtime, which is not a pip
# package: `brew install libomp` (one-time, system-level). Without it,
# `import xgboost` raises XGBoostError at load time — see
# docs/research/baseline_ml_report.md section B.
numpy>=1.26
pandas>=2.1
pytest>=8.0
matplotlib>=3.8
scikit-learn>=1.4
xgboost>=2.0
shap>=0.45
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27

# AI Orchestrator (backend/ai/) — optional real provider. The app starts and
# every other Sentinel feature works with this package absent or with no API
# key configured; backend/ai/providers/openai_provider.py imports it lazily
# and the assistant falls back to a clear "provider unavailable" response
# rather than crashing. See docs/architecture/ai_orchestrator.md.
openai>=1.0
